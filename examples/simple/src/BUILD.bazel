package(default_visibility = ["//visibility:public"])

load("@bazel_typescript//:defs.bzl", "ts_library")

ts_library(
  name = "main",
  srcs = [
    "main.ts",
  ],
  deps = [
    "//src/util:constants",
    "//:packages",
  ],
)
